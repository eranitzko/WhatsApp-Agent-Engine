# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# Settings
pydantic-settings==2.7.0

# Database
sqlalchemy==2.0.36
alembic==1.14.0

# HTTP client (bridge communication)
httpx==0.28.1

# Image processing
Pillow>=11.1.0

# Cloud storage (Cloudflare R2 via S3-compatible API)
boto3==1.36.0

# AI vision (Gemini — invoice OCR only)
google-generativeai==0.8.3

# Agent reasoning (Claude — natural language, tool use, prompt caching)
anthropic==0.49.0

# Currency data
yfinance==0.2.51

# Reports
reportlab==4.2.5
openpyxl==3.1.5
pdfplumber==0.11.9

# RTL / Hebrew text support for PDF
python-bidi==0.6.6

# QR code generation (for WhatsApp re-auth email notifications)
qrcode==8.0

notion-client>=2.2.1
pytest-asyncio>=0.23.0

# Task scheduling (reminders)
apscheduler==3.10.4

# Admin panel auth
python-jose[cryptography]==3.3.0

# Cron expression evaluation (automation engine)
croniter==3.0.3
